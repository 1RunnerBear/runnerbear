name: RunnerBear v5.9 one-time patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RunnerBear v5.9 is already enabled. This one-time workflow is intentionally disabled."
