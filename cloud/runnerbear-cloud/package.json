{
  "name": "runnerbear-cloud",
  "private": true,
  "version": "10.29.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check src/index-v1027.js && cd ../.. && node --test tests/v1027-*.test.cjs tests/e2e/*.spec.mjs",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "db:smoke:remote": "wrangler d1 execute DB --remote --command=\"SELECT name FROM sqlite_master WHERE type='table' AND name IN ('rb_users','rb_plan_revisions','rb_coach_decisions','rb_sync_operations') ORDER BY name\" --json",
    "access:configure": "node scripts/configure-access.mjs"
  },
  "dependencies": {
    "jose": "6.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
