const test=require('node:test');const assert=require('node:assert/strict');
const now='2026-08-21T08:00:00.000Z',workout={workoutId:'wo-1',workoutType:'easy'};
test('stale health never yields green high confidence',async()=>{const {decideCoach}=await import('../cloud/runnerbear-cloud/src/v1026/coach-engine.js');const d=decideCoach({planRevisionId:'pr-1',todayWorkout:workout,health:{measuredAt:'2026-08-19T06:00:00Z',syncedAt:'2026-08-19T08:00:00Z'},now});assert.equal(d.type,'wait_for_data');assert.equal(d.confidence,'low')});
test('two recovery flags reduce dose by no more than 20 percent',async()=>{const {decideCoach,safeAutoAllowed}=await import('../cloud/runnerbear-cloud/src/v1026/coach-engine.js');const d=decideCoach({planRevisionId:'pr-1',todayWorkout:workout,health:{measuredAt:'2026-08-21T06:00:00Z',syncedAt:'2026-08-21T07:30:00Z',hrvRatio:.8,sleepRatio:.8,rhrDelta:1,sampleCount:15},now});assert.equal(d.type,'reduce');assert.equal(d.action.change.reductionPercent,20);assert.equal(safeAutoAllowed(d),true)});
test('illness requires input and is never auto applied',async()=>{const {decideCoach,safeAutoAllowed}=await import('../cloud/runnerbear-cloud/src/v1026/coach-engine.js');const d=decideCoach({planRevisionId:'pr-1',todayWorkout:workout,health:{measuredAt:'2026-08-21T06:00:00Z',syncedAt:'2026-08-21T07:30:00Z'},subjective:{illness:true},now});assert.equal(d.type,'needs_input');assert.equal(safeAutoAllowed(d),false)});
