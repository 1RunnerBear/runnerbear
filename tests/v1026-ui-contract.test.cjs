const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');
test('locked design and coach language remain intact',()=>{const ui=fs.readFileSync('runnerbear-ui-v1031-source.js','utf8'),html=fs.readFileSync('index.html','utf8');assert.match(ui,/Helsedata/);assert.match(ui,/Coach foreslår endringer/);assert.match(ui,/Trygg autopilot aktiv/);assert.match(ui,/diagnostics=!coachLoopUi\(\)/);assert.ok(fs.existsSync('runnerbear-v1026.css'));assert.ok(fs.existsSync('runnerbear-ui-v1026.js'));assert.match(html,/runnerbear-v1031\.css/);assert.match(html,/runnerbear-ui-v1031\.js/)});
test('goal confidence separates the athlete goal from the estimate',()=>{const ui=fs.readFileSync('runnerbear-ui-v1031-source.js','utf8');assert.match(ui,/Foreløpig RunnerBear-estimat/);assert.match(ui,/RunnerBear-estimat/);assert.match(ui,/Hva flyttet vurderingen/);assert.match(ui,/showPreciseCorridor/)});
