const test=require('node:test');const assert=require('node:assert/strict');
test('goal change creates a new goal-shaped plan rather than relabeling old rows',async()=>{const {previewPlan}=await import('../cloud/runnerbear-cloud/src/v1026/plan-engine.js');const config={profile:{baseKm:50},constraints:{runDays:[1,2,3,4,6],qualityDays:[1,4],longRunDay:6,alternativeDays:[0,5],maxRunDays:5,weeklyKmCap:55},goal:{mode:'race',distance:'ten',date:'2026-09-20',name:'10 km'}};const p=previewPlan({currentItems:[{workoutId:'old',localDate:'2026-08-24',workoutType:'easy',sport:'running',plannedDistanceM:5000}],config,fromDate:'2026-08-24',goalChanged:true});assert.equal(p.validation.valid,true);assert.ok(p.rows.length>1);assert.ok(p.rows.some(x=>x.workoutType==='race'&&x.localDate==='2026-09-20'))});
test('goal regeneration preserves completed history and future user locks',async()=>{const {previewPlan}=await import('../cloud/runnerbear-cloud/src/v1026/plan-engine.js'),config={profile:{baseKm:50},constraints:{runDays:[1,2,3,4,6],qualityDays:[1,4],longRunDay:6,alternativeDays:[0,5],maxRunDays:5,weeklyKmCap:55},goal:{mode:'race',distance:'ten',date:'2026-10-03',name:'Ny 10 km'}},currentItems=[{workoutId:'done',localDate:'2026-08-20',status:'completed',sport:'running',workoutType:'quality',title:'Gjennomført',plannedDistanceM:9000},{workoutId:'locked',localDate:'2026-08-26',status:'scheduled',sport:'running',workoutType:'easy',title:'Låst',plannedDistanceM:5000,lockLevel:'user'}],out=previewPlan({currentItems,config,fromDate:'2026-08-24',goalChanged:true});assert.equal(out.rows.find(x=>x.workoutId==='done').title,'Gjennomført');assert.equal(out.rows.find(x=>x.workoutId==='locked').localDate,'2026-08-26')});
